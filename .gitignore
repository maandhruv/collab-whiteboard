# Node / package managers
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Output
.next/
out/
/dist/
/build/

# TypeScript
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Coverage / testing
coverage/
.nyc_output/

# Misc caches
.cache/
.vercel/
.parcel-cache/
.eslintcache
stylelint.cache

# Local data / debug
/tmp/
*.local
*.pid

# SQLite dev database
prisma/dev.db
prisma/dev.db-journal
prisma/*.db

# Yjs persisted data (if added later)
**/yjs-dump-*.bin

# Locks (except canonical lock file already tracked if desired)
# If you only want one lockfile, ignore others:
yarn.lock
pnpm-lock.yaml
